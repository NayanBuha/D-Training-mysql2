CREATE TABLE `customer`.`employee` (  
  `Id` INT NOT NULL,
  `first_name` VARCHAR(250),
  `last_name` VARCHAR(250),
  `age` INT(20),
  `mobile_number` INT(20),
  `address` VARCHAR(250),
  PRIMARY KEY (`Id`) 
);
